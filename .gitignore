# ==========================================
# STM32 (Keil uVision + STM32CubeMX + VSCode) 专用忽略规则
# ==========================================

# ------------------------------------------
# 1. Keil uVision 编译产物与中间文件
# ------------------------------------------
*.o             # 目标文件
*.d             # 依赖文件
*.axf           # Keil ELF格式可执行文件
*.elf           # 通用ELF文件
*.hex           # 最终烧录文件
*.bin           # 二进制文件
*.crf           # 交叉引用文件
*.htm           # 编译生成的网页报告
*.lnp           # 链接器临时文件
*.sct           # 分散加载文件（通常自动生成）
*.dep           # 依赖文件
*.i             # 预处理后的文件
*.ii            # C++预处理后的文件
*.lst           # 列表文件
*.map           # 内存映射文件
*.md            # Keil生成的说明文档
*.bak           # 备份文件
*.tmp           # 临时文件
*.log           # 日志文件

# Keil 用户特定配置（窗口布局、断点、调试器设置等，无需共享）
*.uvoptx
*.uvguix

# Keil 编译输出目录
Objects/
Listings/

# ------------------------------------------
# 2. STM32CubeMX 生成的临时文件
# ------------------------------------------
Backup/         # CubeMX自动备份的旧版本文件
*.ioc.bak       # CubeMX项目文件的备份

# ------------------------------------------
# 3. VSCode 编辑器配置
# ------------------------------------------
# 注意：如果团队需要统一的 include 路径或调试配置，
# 可以删除下面这行，或者手动添加 `!.vscode/c_cpp_properties.json` 来保留特定文件
.vscode/
*.code-workspace

# ------------------------------------------
# 4. 操作系统通用垃圾文件
# ------------------------------------------
.DS_Store       # macOS  Finder 配置
Thumbs.db       # Windows 缩略图缓存
desktop.ini     # Windows 文件夹配置